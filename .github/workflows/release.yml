name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  setup:
    name: 🔧 Setup & Cache
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup Workspace
        uses: ./.github/actions/setup-workspace
  
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/restore-workspace
      - name: Run build
        run: pnpm turbo run build

  versionning:
    name: ✨ Versionning
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/restore-workspace
      - name: Run versionning
        run: echo "Versionning"

  tags:
    name: 🎨 Tags
    runs-on: ubuntu-latest
    needs: versionning
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/restore-workspace
      - name: Run tags
        run: echo "Tags"
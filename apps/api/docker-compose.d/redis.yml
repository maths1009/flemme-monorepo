services:
  redis:
    container_name: flemme_redis
    image: redis:alpine
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis_password}
    ports:
      - '${REDIS_PORT}:6379'
    volumes:
      - redis_data:/data
